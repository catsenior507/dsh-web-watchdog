# dsh-web-watchdog bundle patch: inserts its public dsh.client entry into the web plugin roster.
# Install with `dsh plugin --profile web add <path-or-git-url>`.
- insert:
    - id: ui-dsh-web-watchdog
      name: '@dsh-external/dsh-client-plugin-web-watchdog'
